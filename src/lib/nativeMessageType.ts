export interface NativeMessageData {
  data: NativeMessage;
}

export interface NativeMessage {
  accessToken: string;
  assessment: string;
  birthDay: string;
  birthMonth: string;
  birthYear: string;
  chartNo: string;
  exportOptionType: string;
  id: number;
}
